// mev-cli is a binary-only crate; see src/main.rs. This empty lib.rs is a
// leftover stub that couldn't be deleted directly in this session (the
// sandbox blocked `rm` for this path) -- safe to `rm` manually, it has no
// effect on the build either way (cargo just compiles an empty lib target).
